{
  "name": "pense-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --host 127.0.0.1 --port 8080",
    "build": "nuxt generate",
    "build:ssr": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview --host 127.0.0.1 --port 8080"
  },
  "dependencies": {
    "nuxt": "^4.4.8"
  }
}
